---
phase: 07-validation-rollout
plan: 01
type: execute
wave: 1
depends_on:
  - phase-06-multi-tenant-routing
files_modified: []
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Integration tests, staged switch-over, old DB decommissioning.

This phase becomes active after Phase 6 (Multi-Tenant DB Routing) is complete.

**Scope**:
- Integration tests for tenant DB routing flow
- Staged rollout plan for production switch-over
- Old database decommissioning procedure
- Performance benchmarks under tenant DB routing
</objective>

<tasks>

<task type="automated">
  <name>Create Tenant Lifecycle Integration Test Suite</name>
  <files>
    - backend/internal/database/tenant/integration_test.go
    - backend/internal/database/tenant/testcontainers_setup.go
  </files>
  <action>
    Create a new integration test suite using testcontainers-go to verify the full tenant lifecycle:
    1. Provisioning: Create a new tenant database
    2. Migration: Apply all migrations to the tenant DB
    3. Routing: Verify JWT → TenantDBResolver → TenantRepositories chain
    4. Isolation: Verify School A cannot access School B's data
    
    Use the existing `isolation_test.go` as a reference for patterns.
    Add build tag: `//go:build integration`
  </action>
  <done>false</done>
</task>

<task type="automated">
  <name>Create API Compatibility Verification Tests</name>
  <files>
    - backend/internal/api/compatibility_test.go
  </files>
  <action>
    Create tests that verify the new Go API responses match expected formats:
    1. UUID vs Int ID handling across all critical endpoints
    2. Response schema validation for key modules (Academic, Finance, HR)
    3. Error response format consistency
    
    Focus on endpoints that were modified in Phase 6.
  </action>
  <done>false</done>
</task>

<task type="automated">
  <name>Create Performance Benchmark Suite</name>
  <files>
    - backend/benchmarks/tenant_routing_bench_test.go
  </files>
  <action>
    Create benchmarks to measure routing overhead:
    1. Benchmark request latency with vs without tenant resolution
    2. Benchmark connection pool behavior under concurrent tenant requests
    3. Benchmark migration time for N tenant databases
    
    Use Go's built-in benchmarking framework.
  </action>
  <done>false</done>
</task>

<task type="documentation">
  <name>Create Staged Rollout Runbook</name>
  <files>
    - .planning/phases/07-validation-rollout/07-ROLLOUT.md
  </files>
  <action>
    Create a detailed operational runbook for production rollout:
    1. Internal Beta: Staging environment setup and pilot school migration
    2. Shadow Phase: Traffic mirroring configuration and comparison methodology
    3. Canary Rollout: DNS switch procedure for 10% of schools
    4. Full Cut-over: Complete migration and legacy API deprecation
    
    Include rollback procedures for each stage.
  </action>
  <done>false</done>
</task>

<task type="documentation">
  <name>Create Legacy DB Decommissioning Procedure</name>
  <files>
    - .planning/phases/07-validation-rollout/07-DECOMMISSION.md
  </files>
  <action>
    Document the safe decommissioning of the legacy database:
    1. Pre-requisites checklist (all schools migrated, backups verified)
    2. Step-by-step read-only transition
    3. Final backup and archival procedure
    4. Database deletion and cleanup
    
    Include verification steps at each stage.
  </action>
  <done>false</done>
</task>

<task type="automated">
  <name>Add Tenant Provisioning E2E Test</name>
  <files>
    - backend/scripts/test_tenant_lifecycle.sh
    - backend/Makefile (add tenant-lifecycle target)
  </files>
  <action>
    Create a shell-based E2E test that exercises the full tenant lifecycle:
    1. Start PostgreSQL via Docker
    2. Run provisioning for a test school
    3. Verify database creation
    4. Run migrations
    5. Query via API to verify routing works
    6. Cleanup
    
    Add a Makefile target: `make tenant-lifecycle`
  </action>
  <done>false</done>
</task>

</tasks>
