---
phase: 08-error-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/modules/reports/service.go
  - backend/internal/modules/communication/service.go
  - backend/internal/modules/admission/service.go
  - backend/internal/modules/cba/service.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "All GORM query errors in report service are logged with logger.Warnf instead of silently discarded"
    - "All GORM update/enqueue errors in communication service are returned to callers"
    - "All GORM update errors in admission service are returned to callers"
    - "All GORM update errors in CBA service are returned to callers"
    - "go build ./... and go vet ./... pass after all changes"
  artifacts:
    - path: "backend/internal/modules/reports/service.go"
      provides: "All _ = discarded errors replaced with logger.Warnf + continue"
      contains: "logger.Warnf"
    - path: "backend/internal/modules/communication/service.go"
      provides: "Queue enqueue and campaign update errors returned instead of discarded"
    - path: "backend/internal/modules/admission/service.go"
      provides: "Application update errors returned instead of discarded"
    - path: "backend/internal/modules/cba/service.go"
      provides: "Exam session/answer update errors returned instead of discarded"
  key_links:
    - from: "reports/service.go"
      to: "pkg/logger"
      via: "logger.Warnf on query failure"
      pattern: "logger\\.Warnf"
    - from: "communication/service.go"
      to: "queueClient.Enqueue()"
      via: "error handling instead of _ ="
    - from: "cba/service.go"
      to: "repo.UpdateExamAnswer/UpdateExamSession"
      via: "error handling instead of _ ="
---

<objective>
Fix all silently discarded errors across 4 service files. Reports queries get `logger.Warnf` + continue (best-effort analytics). Communication/Admission/CBA mutations get proper error returns (state changes must not be silently lost).

Purpose: Eliminate silent failure paths that could cause data loss or incorrect report generation without visibility.
Output: 4 modified service files with all `_ =` GORM/discard patterns resolved.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

The project uses `github.com/playbits/schoolcare-v2/pkg/logger` which wraps `slog`.
Available functions: `logger.Warnf(format string, args ...any)`, `logger.Errorf(format string, args ...any)`.

Existing pattern already established in score/service.go line 573:
```go
logger.Warnf("grade item %d (%s): score %.2f exceeds max %.2f, capping at max", ...)
```

Reports strategy: queries are best-effort analytics — LOG and continue, do NOT return error.
All other modules: state mutations — RETURN the error.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix discarded errors in reports/service.go (13 patterns, log + continue)</name>
  <files>backend/internal/modules/reports/service.go</files>
  <action>
    Replace all 13 `_ = s.db.WithContext(ctx)` discard patterns across 4 compute*Report methods with:
    - Store the error from each GORM chain
    - Log with `logger.Warnf("report query [descriptive_name]: %v", err)` when error is non-nil
    - Continue execution (do NOT return error — best-effort analytics)

    Specifically:

    **computeEnrollmentReport** (lines 166-192):
    1. Line 166: `_ = s.db.WithContext(ctx).Table("role_user")...Count(&totalStudents)`
       → `if err := s.db.WithContext(ctx).Table("role_user")...Count(&totalStudents).Error; err != nil { logger.Warnf("enrollment total students query: %v", err) }`
    2. Line 174: `_ = s.db.WithContext(ctx).Model(&models.Enrollment{})...Count(&newEnrollments)`
       → same pattern, log "enrollment new count query"
    3. Line 184: `_ = s.db.WithContext(ctx).Table("role_user")...Scan(&byLevel)`
       → same pattern, log "enrollment by_level query"

    **computeAttendanceReport** (lines 213-237):
    4. Line 213: `_ = s.db.WithContext(ctx).Model(&models.Attendance{})...Scan(&overall)`
    5. Line 230: `_ = s.db.WithContext(ctx).Model(&models.Attendance{})...Scan(&byClass)`

    **computeFinancialReport** (lines 265-295):
    6. Line 265: total revenue Scan
    7. Line 272: outstanding fees Scan
    8. Line 279: totalBills Count
    9. Line 289: byFeeType Scan

    **computeAcademicReport** (lines 316-346):
    10. Line 316: bySubject Scan
    11. Line 325: byClass Scan
    12. Line 335: totalScores Count
    13. Line 342: overallAvg Scan

    IMPORTANT: When calling `Count()` on a GORM query, the chain returns `*gorm.DB` not an error directly.
    Pattern to use:
    ```go
    var count int64
    if err := tx.Count(&count).Error; err != nil {
        logger.Warnf("report query name: %v", err)
    }
    // Or for Scan chains:
    var result Type
    if err := tx.Scan(&result).Error; err != nil {
        logger.Warnf("report query name: %v", err)
    }
    ```
    Since the existing code uses `_ = s.db.WithContext(ctx)[chain].Count(&var)` or `_ = s.db.WithContext(ctx)[chain].Scan(&var)`,
    restructure to capture `.Error` from the terminal call.

    Add import: `"github.com/playbits/schoolcare-v2/pkg/logger"` if not already present.

    Required: `go build ./...` and `go vet ./...` must pass.
  </action>
  <verify>
    <automated>cd /home/playbit/Playbit/academio/backend && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    All 13 discard patterns replaced with logger.Warnf. Build and vet pass. No regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix discarded errors in communication/service.go (3 patterns, return error)</name>
  <files>backend/internal/modules/communication/service.go</files>
  <action>
    Replace 3 `_ =` discard patterns with proper error returns or logging:

    1. **Line 277** (`SendBulk`): `_ = s.queueClient.Enqueue(task)`
       - This is inside a loop iteration where `taskErr == nil` was checked
       - Change to: store error, log with `logger.Errorf("send bulk enqueue: %v", err)`, count as failed but continue loop
       - This is a queuing operation in a bulk send — fail individual, not whole batch

    2. **Line 533** (`SendBroadcast`): `_ = s.queueClient.Enqueue(task)`
       - This is inside a loop iteration inside SendBroadcast
       - Same pattern as above: log error, continue loop

    3. **Line 553** (`SendBroadcast`): `_ = tempRepo.UpdateCampaign(ctx, campaign)`
       - This updates the campaign with final sent/failed counts after broadcast
       - Change to: `if err := tempRepo.UpdateCampaign(ctx, campaign); err != nil { logger.Errorf("broadcast update campaign counts: %v", err) }`
       - Log but don't return — the messages were already sent

    Add `"github.com/playbits/schoolcare-v2/pkg/logger"` to imports.

    Required: `go build ./...` and `go vet ./...` must pass.
  </action>
  <verify>
    <automated>cd /home/playbit/Playbit/academio/backend && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    All 3 discard patterns replaced. Build and vet pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix discarded errors in admission + cba services (4 patterns, return error)</name>
  <files>
    backend/internal/modules/admission/service.go
    backend/internal/modules/cba/service.go
  </files>
  <action>
    Replace all `_ =` discard patterns with proper error returns:

    **admission/service.go:**

    1. **Line 520** (`CreateExamResult`): `_ = tempRepo.UpdateApplication(ctx, app)`
       - This is a state mutation: application status changes from screening→offered
       - MUST return this error. Change to:
         ```go
         if err := tempRepo.UpdateApplication(ctx, app); err != nil {
             return nil, fmt.Errorf("update application status after exam result: %w", err)
         }
         ```

    2. **Line 573** (`CreateOffer`): `_ = tempRepo.UpdateApplication(ctx, app)`
       - Same pattern: application status changes to "offered"
       - MUST return this error. Change to:
         ```go
         if err := tempRepo.UpdateApplication(ctx, app); err != nil {
             return nil, fmt.Errorf("update application status after offer: %w", err)
         }
         ```

    **cba/service.go:**

    3. **Line 631** (`ResumeExam`): `_ = tempRepo.UpdateExamSession(ctx, session)`
       - Session status changed to "submitted" due to timeout
       - MUST return. Change to:
         ```go
         if err := tempRepo.UpdateExamSession(ctx, session); err != nil {
             return nil, fmt.Errorf("timeout submit exam session: %w", err)
         }
         ```

    4. **Line 697** (`gradeExamAnswers`): `_ = repo.UpdateExamAnswer(ctx, &answers[i])`
       - Inside a loop — individual answer grade persistence
       - Log but continue (don't fail whole grading for one answer):
         ```go
         if err := repo.UpdateExamAnswer(ctx, &answers[i]); err != nil {
             logger.Errorf("save graded answer %d: %v", answers[i].ID, err)
         }
         ```

    5. **Line 980** (`GradeAnswer` handler): `_ = tempRepo.UpdateExamSession(ctx, session)`
       - After recalculating session score, updating session to "graded" state
       - MUST return. Change to:
         ```go
         if err := tempRepo.UpdateExamSession(ctx, session); err != nil {
             return nil, fmt.Errorf("update session after grading: %w", err)
         }
         ```

    Add `"github.com/playbits/schoolcare-v2/pkg/logger"` to imports where needed.

    Required: `go build ./...` and `go vet ./...` must pass.
  </action>
  <verify>
    <automated>cd /home/playbit/Playbit/academio/backend && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    All 5 discard patterns replaced (2 admission + 3 CBA). Build and vet pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service layer → database | Best-effort report queries silently failing could produce misleading analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | reports/service.go | mitigate | Log warnings when analytics queries fail so ops can identify stale report data |
| T-08-02 | Tampering | admission/cba/communication | mitigate | Return errors from state-mutation queries so silent failures don't corrupt application/exam/communication state |
</threat_model>

<verification>
1. `cd /home/playbit/Playbit/academio/backend && go build ./...` passes
2. `go vet ./...` passes
3. No `_ =` GORM discard patterns remain in the 4 modified files (grep-check: `grep -n '_ =.*\.\(Table\|Model\|Exec\|Count\|Scan\)'` on each file should return empty)
</verification>

<success_criteria>
- All 13 reports discard patterns logged with `logger.Warnf` instead of discarded
- All 8 communication/admission/CBA discard patterns either returned as errors or logged with `logger.Errorf`
- Build and vet pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-error-propagation/08-01-SUMMARY.md`
</output>
