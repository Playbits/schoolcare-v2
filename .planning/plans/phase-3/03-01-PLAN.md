---
phase: 03-communication-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/database/models/school.go                      # NEW MessageAttachment model
  - backend/internal/database/migrations/school/school.go           # NEW AutoMigrate for MessageAttachment
  - backend/internal/modules/messages/dto.go                        # NEW attachment DTOs, file upload DTO
  - backend/internal/modules/messages/service.go                    # NEW attachment handling, WS publish on send
  - backend/internal/modules/messages/handler.go                    # NEW attachment upload handler, WS event wiring
  - backend/internal/modules/messages/repository.go                 # NEW attachment CRUD methods
  - backend/internal/ws/router.go                                   # NEW message event types
  - backend/internal/router/setup.go                                # NEW WS wiring for messages
autonomous: true
requirements:
  - COMM-01
  - COMM-02

must_haves:
  truths:
    - "User can attach files to messages (images, PDFs, docs) — uploaded via multipart POST, stored as MessageAttachment records"
    - "When a message is sent, a real-time event is pushed to the recipient(s) via WebSocket to their user room"
    - "Sender sees message as 'sent' immediately; recipient sees it arrive in real-time with no page refresh"
    - "Read receipts are published via WebSocket when recipient opens/marks a message as read"
    - "Existing REST endpoints continue to work unchanged — WS is additive"
  artifacts:
    - path: "backend/internal/database/models/school.go"
      provides: "MessageAttachment model with message_id, file_name, file_path, mime_type, size, uploaded_by"
      contains: "type MessageAttachment struct"
    - path: "backend/internal/database/migrations/school/school.go"
      provides: "AutoMigrate entry for MessageAttachment"
      contains: "&models.MessageAttachment{}"
    - path: "backend/internal/modules/messages/dto.go"
      provides: "UploadAttachmentRequest/Response, MessageWithAttachmentsResponse"
      contains: "type UploadAttachmentResponse struct"
    - path: "backend/internal/modules/messages/repository.go"
      provides: "CreateAttachment, FindAttachmentsByMessageID, DeleteAttachment"
      contains: "func (r *MessageRepository) CreateAttachment"
    - path: "backend/internal/modules/messages/service.go"
      provides: "UploadAttachment, GetMessageWithAttachments, publishMessageViaWS, publishReadReceipt"
      contains: "func (s *MessageService) UploadAttachment"
    - path: "backend/internal/modules/messages/handler.go"
      provides: "POST /messages/:id/attachments upload endpoint"
      contains: "func (h *MessageHandler) UploadAttachment"
    - path: "backend/internal/ws/router.go"
      provides: "message:new, message:read event types"
      contains: "MessageNewEvent"

execution:
  - step: "Add MessageAttachment model to school.go"
    details: "Fields: BaseModel, MessageID, FileName, FilePath, MimeType, Size, UploadedBy. Table: message_attachments."
  - step: "Add AutoMigrate to migrations/school/school.go"
    details: "Add &models.MessageAttachment{} to existing group."
  - step: "Add attachment DTOs to messages/dto.go"
    details: "UploadAttachmentResponse with ID, file_name, mime_type, size, url, created_at."
  - step: "Add attachment repository methods"
    details: "CreateAttachment, FindAttachmentsByMessageID, DeleteAttachment on MessageRepository."
  - step: "Add attachment service methods + WS publish"
    details: |
      UploadAttachment(ctx, tenantDB, messageID, fileHeader) — saves file, creates record.
      publishMessageToRecipient(ctx, tenantDB, message) — sends WS message:new event to each recipient's user room.
      publishReadReceipt(ctx, tenantDB, messageID, userID) — sends WS message:read event.
      Wire publishMessageToRecipient call into existing SendMessage after DB insert.
  - step: "Add attachment upload handler"
    details: "POST /messages/:id/attachments — multipart form, max 10MB, saved to school storage path."
  - step: "Add WS event types to ws/router.go"
    details: "MessageNewEvent = 'message:new', MessageReadEvent = 'message:read' constants."
  - step: "Inject ws.Hub into MessageService"
    details: "Add wsHub field to MessageService, update constructor. Wire in setup.go."
