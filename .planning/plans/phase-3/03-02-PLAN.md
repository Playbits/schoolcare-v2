---
phase: 03-communication-calendar
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/internal/modules/parentdashboard/dto.go                # NEW inbox/conversation DTOs
  - backend/internal/modules/parentdashboard/handler.go            # NEW inbox/unread/conversation endpoints
  - backend/internal/modules/parentdashboard/service.go            # NEW inbox methods, WS unread push
  - backend/internal/modules/parentdashboard/repository.go         # NEW inbox queries
  - backend/internal/router/router.go                              # NEW parent inbox routes
autonomous: true
requirements:
  - COMM-02

must_haves:
  truths:
    - "Parent can see unread message count on their dashboard (via existing GET /parent/dashboard)"
    - "Parent can view a list of conversations involving their children"
    - "Parent can open a conversation and see threaded messages with read receipts"
    - "New message to parent triggers WS event to update unread count in real-time"
    - "Conversation list shows last message preview, sender name, timestamp, unread count"
  artifacts:
    - path: "backend/internal/modules/parentdashboard/dto.go"
      provides: "ParentConversationResponse, ParentMessageResponse, UnreadCountResponse"
      contains: "type ParentConversationResponse struct"
    - path: "backend/internal/modules/parentdashboard/handler.go"
      provides: "GET /parent/inbox, GET /parent/inbox/conversations, GET /parent/inbox/conversations/:id"
      contains: "func (h *ParentDashboardHandler) ListInbox"
    - path: "backend/internal/modules/parentdashboard/service.go"
      provides: "GetInbox, GetConversations, GetConversationMessages, GetUnreadCount, resolveChildUserIDs"
      contains: "func (s *ParentDashboardService) GetInbox"
    - path: "backend/internal/modules/parentdashboard/repository.go"
      provides: "FindMessagesForChildren, FindConversationsForChildren, CountUnreadForChildren"
      contains: "func (r *ParentDashboardRepository) FindMessagesForChildren"

execution:
  - step: "Add inbox DTOs to parentdashboard/dto.go"
    details: |
      ParentInboxResponse: conversations []ParentConversationSummary, unread_count int
      ParentConversationSummary: id, participants, last_preview, last_sender, last_time, unread_count
      ParentMessageResponse: id, sender_name, subject, body, attachments, read, created_at
  - step: "Add repository methods"
    details: |
      FindMessagesForChildren(ctx, tenantDB, childUserIDs, page, limit) — messages where recipient IN childUserIDs
      FindConversationsForChildren(ctx, tenantDB, childUserIDs) — grouped conversations
      CountUnreadForChildren(ctx, tenantDB, childUserIDs) — unread count
  - step: "Add inbox service methods"
    details: |
      GetInbox(ctx, tenantDB, parentUserID) — resolves children, returns inbox with unread count
      GetConversations(ctx, tenantDB, parentUserID) — grouped conversation list
      GetConversationMessages(ctx, tenantDB, parentUserID, conversationID) — threaded messages
  - step: "Add inbox handler endpoints"
    details: |
      GET /parent/inbox — returns inbox summary (unread count + recent conversations)
      GET /parent/inbox/conversations — full conversation list
      GET /parent/inbox/conversations/:id — messages in a conversation
  - step: "Add routes to router.go"
    details: "Register 3 new endpoints under existing parent authGroup."
