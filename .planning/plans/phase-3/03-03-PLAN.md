---
phase: 03-communication-calendar
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/internal/modules/timetable/dto.go                      # NEW iCal query param DTOs (if any)
  - backend/internal/modules/timetable/service.go                  # NEW ExportICal method
  - backend/internal/modules/timetable/handler.go                  # NEW iCal download handler
  - backend/internal/router/router.go                              # NEW GET /timetables/ical route
autonomous: true
requirements:
  - COMM-03

must_haves:
  truths:
    - "User can download timetable as a valid RFC 5545 iCalendar (.ics) file"
    - "iCal file imports correctly into Google Calendar, Apple Calendar, and Outlook"
    - "Download is filtered by session_id (required) and optionally by level_id"
    - "Each timetable entry becomes a VEVENT with DTSTART, DTEND, SUMMARY, DESCRIPTION, LOCATION, RRULE (if weekly recurring)"
    - "Response has Content-Type: text/calendar and Content-Disposition: attachment headers"
  artifacts:
    - path: "backend/internal/modules/timetable/service.go"
      provides: "buildICalendar method — generates RFC 5545 iCal text from timetable entries"
      contains: "func (s *TimetableService) buildICalendar"
    - path: "backend/internal/modules/timetable/handler.go"
      provides: "GET /timetables/ical endpoint — returns .ics file"
      contains: "func (h *TimetableHandler) ExportICal"

execution:
  - step: "Add buildICalendar to timetable/service.go"
    details: |
      Method: buildICalendar(entries []models.Timetable) (string, error)
      Generates VCALENDAR wrapper with VERSION:2.0, PRODID, CALSCALE:GREGORIAN
      Each entry becomes VEVENT with:
        DTSTART;TZID=Africa/Lagos:YYYYMMDDTHHMMSS
        DTEND;TZID=Africa/Lagos:YYYYMMDDTHHMMSS
        RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR (if recurring)
        SUMMARY: {Subject Name} - {Teacher Name}
        DESCRIPTION: {entry name or "Timetable Entry"}
        LOCATION: {location or "-"}
        UID: {uuid}@academio
      No external dependencies — pure string building with fmt.Sprintf.
  - step: "Add ExportICal handler to timetable/handler.go"
    details: |
      GET /timetables/ical?session_id=&level_id=
      Fetches timetable entries via existing ListCalendarTimetables
      Calls buildICalendar
      c.Header("Content-Type", "text/calendar; charset=utf-8")
      c.Header("Content-Disposition", "attachment; filename=timetable.ics")
      c.String(200, icalContent)
  - step: "Add route to router.go"
    details: "timetableGroup.GET(\"/ical\", timetableHandler.ExportICal) — placed before :id routes to avoid conflict"
