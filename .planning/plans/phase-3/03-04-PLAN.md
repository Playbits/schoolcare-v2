---
phase: 03-communication-calendar
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/database/models/school.go                      # NEW ContactPreference model
  - backend/internal/database/migrations/school/school.go           # NEW AutoMigrate for ContactPreference
  - backend/internal/communication/provider.go                       # NEW WhatsApp provider registration
  - backend/internal/communication/sms.go                            # NEW WhatsApp-specific send path
  - backend/internal/communication/twilio.go                         # NEW WhatsApp support (whatsapp: prefix on From)
  - backend/internal/modules/communication/service.go                # NEW channel preference methods
  - backend/internal/modules/user/dto.go                             # NEW contact preference DTOs
  - backend/internal/modules/user/handler.go                         # NEW contact preference endpoints
  - backend/internal/modules/user/service.go                         # NEW contact preference CRUD
  - backend/internal/modules/user/repository.go                      # NEW contact preference queries
  - backend/internal/router/router.go                                # NEW preference routes
  - backend/internal/router/setup.go                                 # NEW provider wiring
autonomous: true
requirements:
  - COMM-04

must_haves:
  truths:
    - "Admin can send notifications via WhatsApp channel alongside existing email/SMS"
    - "WhatsApp uses Twilio WhatsApp API (same REST endpoint, From: 'whatsapp:+1234')"
    - "Per-parent channel preference (email/sms/whatsapp/in-app) with automatic fallback chain"
    - "Default channel order: in-app → email → whatsapp → sms"
    - "User can update their own channel preference via API"
    - "Existing SendMessage/SendBulk endpoints accept 'channel: whatsapp' to send via WhatsApp"
  artifacts:
    - path: "backend/internal/database/models/school.go"
      provides: "ContactPreference model with user_id, channel (primary), fallback_channels (JSON array), is_opted_in"
      contains: "type ContactPreference struct"
    - path: "backend/internal/communication/twilio.go"
      provides: "WhatsApp send support — same Twilio API, From with whatsapp: prefix"
      contains: "func (p *TwilioSMSProvider) SendWhatsApp"
    - path: "backend/internal/modules/communication/service.go"
      provides: "SendWithChannelPreference — resolves per-user channel, sends via appropriate provider with fallback"
      contains: "func (s *CommunicationService) SendWithChannelPreference"
    - path: "backend/internal/modules/user/service.go"
      provides: "GetContactPreference, SetContactPreference, UpdateContactPreference"
      contains: "func (s *UserService) SetContactPreference"

execution:
  - step: "Add ContactPreference model to school.go"
    details: |
      Fields: BaseModel, UserID (unique), PrimaryChannel string, FallbackChannels *string (JSON array),
      IsOptedIn bool default true, UpdatedAt.
      Table: contact_preferences.
  - step: "Add AutoMigrate to migrations/school/school.go"
    details: "Add &models.ContactPreference{} to existing group."
  - step: "Add WhatsApp support to Twilio provider"
    details: |
      Add SendWhatsApp method: same as Send but prepends "whatsapp:" to the From number.
      Update ProviderFactory to register a "whatsapp" provider alias pointing to same Twilio provider.
      Add WhatsAppProvider interface or extend SMSProvider.
  - step: "Add channel preference repository methods"
    details: |
      FindByUserID, Upsert — stored in tenant schema.
  - step: "Add channel preference service methods"
    details: |
      GetContactPreference — returns user's preference or default.
      SetContactPreference — creates/updates preference.
      SendWithChannelPreference(userID, subject, body) — resolves channel, sends, falls back on failure.
  - step: "Add channel preference DTOs and handler"
    details: |
      ContactPreferenceResponse, UpdateContactPreferenceRequest.
      GET/PUT /users/contact-preference endpoints.
  - step: "Add routes to router.go"
    details: "Register preference endpoints under /users group. Add 'whatsapp' channel support to communication routes."
