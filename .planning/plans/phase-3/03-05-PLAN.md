---
phase: 03-communication-calendar
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/internal/database/models/conference.go                  # NEW Conference, ConferenceSlot, ConferenceBooking models
  - backend/internal/database/migrations/school/school.go           # NEW AutoMigrate for conference models
  - backend/internal/modules/conference/dto.go                      # NEW DTOs (create/list/booking/availability)
  - backend/internal/modules/conference/handler.go                  # NEW HTTP handlers
  - backend/internal/modules/conference/service.go                  # NEW business logic
  - backend/internal/modules/conference/repository.go               # NEW data access
  - backend/internal/router/router.go                               # NEW conference routes
  - backend/internal/router/setup.go                                # NEW DI wiring
autonomous: true
requirements:
  - COMM-05

must_haves:
  truths:
    - "Teacher/admin can configure conference slots (date, start_time, end_time, max_bookings_per_slot)"
    - "Parent can view available slots for their child's teachers and book a slot"
    - "Parent can cancel their booking within the allowed window"
    - "Teacher can view all bookings for their slots"
    - "Automated reminder is sent to parent 24 hours before the conference (via existing communication module)"
    - "Conference has a status: scheduled, ongoing, completed, cancelled"
  artifacts:
    - path: "backend/internal/database/models/conference.go"
      provides: "Conference, ConferenceSlot, ConferenceBooking GORM models"
      contains: "type Conference struct"
    - path: "backend/internal/modules/conference/dto.go"
      provides: "CreateConferenceRequest, ConferenceResponse, SlotResponse, BookingResponse, SlotAvailabilityResponse"
      contains: "type CreateConferenceRequest struct"
    - path: "backend/internal/modules/conference/repository.go"
      provides: "ConferenceRepository, ConferenceSlotRepository, ConferenceBookingRepository interfaces + impls"
      contains: "type ConferenceRepositoryInterface"
    - path: "backend/internal/modules/conference/service.go"
      provides: "CreateConference, ListConferences, CreateSlots, GetAvailableSlots, BookSlot, CancelBooking, GetTeacherBookings, SendReminders"
      contains: "func (s *ConferenceService) BookSlot"
    - path: "backend/internal/modules/conference/handler.go"
      provides: "CRUD endpoints for conferences, slots, and bookings"
      contains: "func (h *ConferenceHandler) BookSlot"

execution:
  - step: "Create conference models"
    details: |
      Conference: school_id, name, description, date, session_id, status
      ConferenceSlot: conference_id, teacher_id, start_time, end_time, max_bookings, current_bookings
      ConferenceBooking: slot_id, parent_user_id, student_id, status (confirmed/cancelled), confirmed_at, cancelled_at
      All use BaseModel, proper FKs with GORM relations.
  - step: "Add AutoMigrate to migrations/school/school.go"
    details: "Add &models.Conference{}, &models.ConferenceSlot{}, &models.ConferenceBooking{} in new group."
  - step: "Create conference DTOs"
    details: |
      CreateConferenceRequest, UpdateConferenceRequest, ConferenceResponse
      CreateSlotRequest (array of slots with date/time/max), SlotResponse, SlotAvailabilityResponse
      BookSlotRequest, BookingResponse
  - step: "Create conference repository"
    details: |
      ConferenceRepository: Create, FindByID, Update, ListBySchool, ListBySession
      ConferenceSlotRepository: Create, FindByID, BatchCreate, FindByConference, FindAvailableByTeacher
      ConferenceBookingRepository: Create, FindByID, Update, FindBySlot, FindByParent, Cancel, CountBySlot
  - step: "Create conference service"
    details: |
      CreateConference(ctx, tenantDB, schoolID, req) -> Conference
      ListConferences(ctx, tenantDB, schoolID, sessionID) -> []Conference
      CreateSlots(ctx, tenantDB, conferenceID, slots) -> []ConferenceSlot
      GetAvailableSlots(ctx, tenantDB, teacherID, conferenceID) -> []SlotAvailabilityResponse
      BookSlot(ctx, tenantDB, slotID, parentUserID, studentID) -> BookingResponse
      CancelBooking(ctx, tenantDB, bookingID) -> error
      GetTeacherBookings(ctx, tenantDB, teacherID, conferenceID) -> []BookingResponse
      SendReminders(ctx, tenantDB, conferenceID) — enqueues asynq reminders 24h before
  - step: "Create conference handler"
    details: |
      POST /conferences — CreateConference
      GET /conferences — ListConferences (school scope)
      GET /conferences/:id — GetConference
      POST /conferences/:id/slots — CreateSlots (batch)
      GET /conferences/:id/slots — GetAvailableSlots (with ?teacher_id)
      POST /conferences/:id/book — BookSlot
      DELETE /conferences/bookings/:id — CancelBooking
      GET /conferences/:id/teacher-bookings — GetTeacherBookings
  - step: "Wire routes and DI"
    details: |
      setup.go: create ConferenceService + ConferenceHandler
      router.go: conferenceGroup under authGroup with all endpoints
