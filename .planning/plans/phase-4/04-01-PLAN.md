# 04-01: End-of-Year Workflow — Grade Archival & Session Completion

## Goal
Implement grade archiving and session completion as part of the end-of-year workflow.

## Work Done
- Added `archived_scores` table (Group 21 migration) to snapshot student scores at end-of-year
- `ArchivedScore` model stores JSON blob of Score at archive time + assessment/session/level/student/subject references
- No `school_id` column on `ArchivedScore` — denormalized model, school context resolved via relationships
- Fixed: FK migration `2026_07_15_000004` no longer references `archived_scores` (table didn't exist yet)

## Files Changed
- `backend/internal/database/models/school.go` — `ArchivedScore` model
- `backend/internal/database/migrations/school/school.go` — Group 21 + FK ordering fix
